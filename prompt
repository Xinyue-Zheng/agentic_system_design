I already have a pretrained model that produces an embedding for each sample. Based on these embeddings, I can train a lightweight predictor to estimate a target value, and use the prediction error as a possible anomaly score.
The challenge is that I don't have any anomaly labels or ground truth. So I'm trying to figure out what kind of unsupervised or self-supervised anomaly detection framework would make the most sense here.
I'd like to discuss different possible directions rather than immediately settling on one solution. For example, should I rely on prediction error, model the embedding distribution directly, use reconstruction, one-class learning, contrastive learning, or something else? I'd also like to understand what existing work is most closely related and where there might be room for something new
