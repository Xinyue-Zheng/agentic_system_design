# Demo Run Summary: TKT-2026-04-17-0002_20260507T091321Z

Artifact directory:
`artifacts/TKT-2026-04-17-0002_20260507T091321Z/`

This summary is organized for demo slides. Planner is intentionally omitted.

---

## Demo Storyline

- Ticket: Partial outage at `USID_09`, affecting sectors `S0` and `S2`.
- Coverage result: RF backup coverage is mostly adequate for failed sectors.
- KPI result: traffic-layer pressure is critical/unsustainable under base and
  stress forecasts.
- Attribute result: static hardware capability is adequate, but constrained by
  dependency on a single feasible micro site.
- Geo result: no hospital/school escalation; affected areas are park edge and
  residential, with no terrain attenuation flag.
- Assessment result: `P2` with medium confidence.
- Cross-agent verifier result: first-pass `rerun` because Coverage and KPI
  disagree directionally.

---

## Coverage Agent: RF Serviceability

**Question answered:** If `USID_09_S0` and `USID_09_S2` fail, can neighbors
provide usable signal coverage?

**How the artifact answers it:**

- `load_redistribution_verdict = adequate`
- `verdict_scope = failed_sectors_only`
- `coverage_hole_fraction = 0.0238`, below the adequate threshold.
- No weak zones were identified.
- Strong zones:
  - `S0` north zone backed primarily by `USID_25`, `rsrp_p50 = -64.8 dBm`,
    `sinr_regime = mostly_mild`.
  - `S2` southwest zone backed primarily by `USID_25`, `rsrp_p50 = -64.8 dBm`,
    `sinr_regime = mostly_mild`.
- `per_zone` contains only failed sectors:
  - `USID_09_S0`
  - `USID_09_S2`

**Demo takeaway:**

- Coverage Agent concludes the RF layer can mostly support handover for the
  failed sectors.
- The only coverage hole is small and localized near the tower/SW fringe, not a
  broad service-loss region.

---

## Coverage Directional Focus: Sector-to-Space Grounding

**Question answered:** Where are the affected failed-sector zones located?

**How the artifact answers it:**

- Directional focus generated `per_zone` for exactly the failed sectors:
  - `USID_09_S0`
  - `USID_09_S2`
- This confirms the agent did not model a full-site outage and did not
  double-count active sector `S1`.
- The resulting `per_zone` entries are used by Geo Agent to center map analysis
  on affected sector footprints rather than the tower location.

**Demo takeaway:**

- Directional Focus turns sector IDs into geographic analysis targets.
- This is the bridge from RF data to map-based user impact analysis.

---

## KPI Agent: Counterfactual Traffic Pressure

**Question answered:** Can neighbors carry the traffic that failed sectors would
have served?

**How the artifact answers it:**

- Final KPI risk:
  - `overload_risk = critical`
  - `overload_risk_base = high`
  - `overload_risk_stress = critical`
- Lost traffic forecast:
  - historical same-hour mean: `136.43 Mbps`
  - selected base case: `144.68 Mbps`
  - selected stress case: `171.89 Mbps`
  - `baseline_adjustment_factor = 1.06`
- Forecast source selection:
  - base source: `same_daytype_same_hour_mean_mbps`
  - stress source: `same_hour_60d_p90_mbps`
  - reason: weekday same-hour demand is higher than the 60-day mean and the
    outage overlaps peak hours.
- Neighbor pressure:
  - `USID_01`: base high, stress critical.
  - `USID_25`: base high, stress critical.
  - `USID_43`: high in both base and stress.
  - `USID_29` and `USID_45`: moderate.
  - `USID_10`: low base, moderate stress.
- Sustained pressure:
  - `sustained_pressure_verdict = unsustainable`
  - all 11 outage hours overloaded in both base and stress distributions.
- Peak pressure:
  - `peak_hour_verdict = critical`
  - peak window: `17:00-20:00`.

**Demo takeaway:**

- KPI Agent gives the main risk signal: even though RF coverage is adequate,
  historical throughput forecasts suggest sustained pressure on primary
  absorbers.
- This is a traffic/capacity pressure signal, not a direct RF coverage failure.

---

## Attribute / Config Agent: Static Capability With Scenario Constraints

**Question answered:** Do neighbor hardware and technology attributes support
absorbing displaced users?

**How the artifact answers it:**

- Rule-based verdict:
  - `overall_capacity_verdict = adequate`
- Scenario-adjusted view:
  - `verdict = adequate_with_constraints`
  - `confidence_modifier = lower_confidence`
- Neighbor classifications:
  - `USID_25`: feasible
  - `USID_01`: marginal
  - `USID_43`: marginal
  - `USID_29`: marginal
  - `USID_10`: marginal
  - `USID_45`: infeasible
- Key scenario constraint:
  - `USID_25` is the only rule-feasible neighbor, but it is a `20m` micro tower.
  - The artifact requires Coverage `per_zone` validation before treating
    `USID_25` as a reliable primary absorber.
- Distributed support:
  - `USID_01`, `USID_43`, `USID_29`, and `USID_10` provide marginal distributed
    support.
- Technology continuity:
  - `nsa_5g_downgrade_risk.flagged = true`
  - `USID_45` is 4G-only.
  - estimated affected fraction: `0.01`.

**Demo takeaway:**

- Attribute Agent does not simply say hardware is adequate.
- It explains that the adequate verdict is conditional: one feasible micro site
  creates dependency concentration and requires Coverage/KPI validation.
- It also identifies a small but real 5G downgrade risk.

---

## Geo Agent: Real-World User Impact

**Question answered:** What kind of places are affected by the RF zones, and do
they require severity escalation?

**How the artifact answers it:**

- `map_available = true`
- Geographic character: `suburban`
- `USID_09_S0`:
  - land use: `forest`
  - user relevance: `low`
  - evidence: green-shaded Bob Woodruff Park area near sector centroid.
- `USID_09_S2`:
  - land use: `residential`
  - user relevance: `high`
  - evidence: regular suburban street grid and smaller buildings.
- Coverage hole interpretation:
  - the single hole pixel is at the tower-site / Renner Rd / park interface.
  - bulk of `S2` is residential, but the hole itself does not represent a large
    residential population loss.
- Self flags:
  - `terrain_attenuation_active = false`
  - `high_sensitivity_area = false`
- No hospital or school labels were identified in affected zones.

**Demo takeaway:**

- Geo Agent prevents over-escalation: no critical infrastructure was found.
- It also explains why Coverage looks adequate: key backup towers sit in or near
  park/forest territory with likely lower native load.

---

## Assessment Agent: Final Severity

**Question answered:** What is the final operational severity and action after
combining RF, KPI, attribute, and geo evidence?

**How the artifact answers it:**

- Final severity: `P2`
- Confidence: `medium`
- Input verdicts:
  - Coverage: `adequate`
  - KPI: `overload`
  - Config/Attribute: `adequate`
  - Geo escalation: `false`
- Secondary signals:
  - `peak_hour_verdict = critical`
  - `sustained_pressure_verdict = unsustainable`
  - `nsa_5g_downgrade_risk = true`
  - `terrain_attenuation_active = false`
- Recommended action:
  - schedule crew within 4 hours
  - monitor neighbor load
  - alert NOC
  - coordinate 5G service team
  - escalate due to degrading/unsustainable neighbor capacity

**Demo takeaway:**

- Assessment does not upgrade to P1 because Coverage is adequate and Geo has no
  high-sensitivity area.
- It does choose P2 because KPI pressure is sustained/critical and there is a
  5G downgrade risk.
- Confidence is medium because Coverage and KPI point in different directions.

---

## Per-Agent Verification

**Question answered:** Did each specialist artifact follow its own contract?

**Results:**

- Coverage verification: `pass`
  - confirmed `verdict_scope = failed_sectors_only`
  - confirmed `per_zone` exists for `USID_09_S0` and `USID_09_S2`
  - confirmed verdict reasoning cites numeric evidence.
- Config/Attribute verification: `pass`
  - feasibility rules were applied correctly.
  - 5G downgrade evidence names `USID_45`.
  - micro-site limitation for `USID_25` was documented.
- Geo verification: `pass`
  - map available.
  - land-use classifications match map evidence.
  - no unsupported critical infrastructure flag.
- KPI verification: `fail` in the verification artifact.
  - verifier flagged insufficient hedging in `overload_risk_note`.
  - this flag was passed to Cross-Agent Verifier.

**Demo takeaway:**

- The verifier layer surfaces quality issues even when the main KPI artifact
  contains useful pressure analysis.
- In slides, this is a good example of the system auditing its own confidence
  language.

---

## Cross-Agent Verifier

**Question answered:** Are the specialist agents mutually consistent?

**How the artifact answers it:**

- Overall result: `rerun`
- Retry count: `0`
- Major discrepancy:
  - `coverage_kpi = major`
  - label: `signal_capacity_conflict`
- Reason:
  - Coverage says RF redistribution is adequate and low risk.
  - KPI says traffic pressure is overloaded/critical.
  - Config is adequate, which makes proxy mismatch or traffic-model uncertainty
    more likely than pure hardware shortage.
- Minor discrepancy:
  - `kpi_config = minor`
  - label: `config_or_software_bottleneck`
  - hardware has capacity, but KPI forecasts pressure beyond historical
    thresholds.
- Consistent checks:
  - Coverage vs Geo
  - Geo vs Assessment
  - Assessment internal severity logic

**Demo takeaway:**

- The verifier does not hide the core conflict.
- First-pass major conflict triggers a rerun rather than immediate HITL.
- This supports the design story: disagreement becomes an explicit control-flow
  decision.

---

## Reflector

**Question answered:** What should the system remember from this run?

**How the artifact answers it:**

- Entry written: `true`
- Memory store total entries: `5`
- Ground truth accuracy: `correct`
- Lessons:
  - Coverage/KPI signal-capacity conflict is recurring for this site.
  - Spatial proxy post-load factors remain low while KPI forecasts high pressure
    on `USID_01` and `USID_25`.
  - `kpi_config` is now labeled as a possible config/software bottleneck.
  - Rerun path is confirmed: first-pass major conflict triggers KPI rerun; HITL
    is reserved for persistent conflict after retry.

**Demo takeaway:**

- Reflector turns a single run into reusable memory.
- It records that this site has a persistent Coverage/KPI discrepancy pattern,
  which can inform future interpretation and HITL policy.

---

## Slide-Level Conclusion

- RF coverage is mostly adequate for failed sectors.
- Traffic forecast is the strongest risk signal: primary absorbers face
  high/critical pressure across the full outage window.
- Attribute analysis supports hardware adequacy, but only with constraints
  because the sole feasible neighbor is a micro site.
- Geo analysis prevents over-escalation by showing no hospital/school and no
  terrain attenuation flag.
- Final severity is `P2`, not `P1`, because the issue is serious sustained
  capacity pressure rather than complete RF loss or critical-infrastructure
  impact.
- Cross-Agent Verifier correctly detects the key demo point: Coverage and KPI
  disagree, so the system asks for rerun instead of pretending the answer is
  fully settled.
